name: Build Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: asifakramdocker
          password: dckr_pat_RfYVcSwqKkWC0_hB7WI2Bl5sCoQ
      - uses: docker/build-push-action@v6
        with:
          context: ./app
          push: true
          tags: asifakramdocker/web-app:ci-latest
